# Secrets — never commit
scripts/quickshell/calendar/.env

# Generated/cache files
scripts/quickshell/qs_colors.json
scripts/quickshell/colors.json
hyprland.conf.save
# Runtime addon state (machine-specific abs paths; regenerated by plugin_install.sh).
# Also leaks which addons are installed — keep out of the public repo.
addons.conf
float_mode_rules.conf

# Plugin compat — local plan doc
PLUGIN_COMPAT_PLAN.md

# Claude Code / AI tooling
.claude/
.codex
PROJECT_ANALYSIS.md
/home/dxvmxn/.config/hypr/.understand-anything

# Reference / external dotfiles
ref_dots/
techdock/
ActiveSpotDesignProto/
noctalia-plugins/
noctalia-shell/

# Quickshell runtime dirs (generated at runtime)
scripts/quickshell/rmqs/

# Float Mode — sold separately as a paid .qsplugin; keep source OUT of the
# public repo. example-addon stays tracked as the reference package format.
scripts/addons-src/float-mode/
scripts/hypr-dock/
plugins/float-mode/
